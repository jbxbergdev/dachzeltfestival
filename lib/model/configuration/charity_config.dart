class CharityConfig {
  final String titleText;
  final String explanationText;
  final String buttonText;
  final String buttonLink;

  CharityConfig({this.titleText, this.explanationText, this.buttonText, this.buttonLink});
}